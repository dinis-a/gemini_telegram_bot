services:
  gemini_bot:
    build:
      context: .
      dockerfile: Dockerfile
    image: dinisak/gemini_bot
    container_name: gemini_bot
    env_file: .env
    volumes:
      - .env:/app/.env
      - ./logs:/app/logs
    restart: unless-stopped